# TTOBA 프론트엔드 환경 변수 예시

# API 기본 URL
VITE_API_BASE_URL=http://localhost:8000

# 배포 환경에서는 다음과 같이 변경:
# VITE_API_BASE_URL=https://백엔드주소.onrender.com

# 주의사항:
# - 이 파일은 Git에 올려도 안전합니다 (예시용)
# - 실제 환경 변수는 .env.development와 .env.production 사용
# - Render 배포 시에는 Render 대시보드에서 직접 설정